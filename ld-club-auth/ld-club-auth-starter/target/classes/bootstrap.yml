spring:
  application:
    name: ld-club-auth
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.101.65:8848
      discovery:
        namespace: ld-club
        group: ld-club-auth
      config:
        namespace: ld-club
        group: ld-club-auth
        file-extension: yaml
        refresh-enabled: true
        extension-configs:
          - data-id: ld-club-auth
            group: ld-club-auth
            refresh: true

